# 改造清单

## 1. 改造目标

把当前系统从“自然语言风格约束”升级成“设计语法驱动生成”。落地后应满足：

- 同风格：色彩、背景明度、卡片体系、线条体系、图标体系稳定
- 异版式：每页骨架不同，不重复套版
- 同元素语言：设计组件一致
- 异具体图形：图标、流程图、关系图按内容重新实例化

核心原则：

- 版式家族的抽象度要高，不能退化成 `layout_1`、`layout_2` 这类写死编号
- 提示词传给生图模型前需要有压缩层，避免 grammar 越堆越长
- 方案优先通用化，避免为当前样例写死特例

---

## 2. 数据结构改造

### 2.1 扩展 `style_guide`

位置建议放在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:102) 附近的风格分析与归一化链路。

新增字段：

- `style_core`
- `layout_families`
- `element_primitives`
- `variation_policy`
- `negative_rules`
- `prompt_anchor`
- `prompt_compression`

建议结构：

```json
{
  "style_core": {
    "background_tone": "白底或浅蓝灰底",
    "palette": ["深海军蓝", "亮蓝", "浅蓝灰", "白色"],
    "title_style": "深蓝粗体大标题，亮蓝关键词强调",
    "card_style": "浅蓝描边圆角卡片，弱阴影或无阴影",
    "icon_style": "扁平线性图标",
    "line_style": "清晰箭头、虚线反馈链路、编号圆点"
  },
  "layout_families": [
    "grid_n_x_m",
    "timeline_horizontal",
    "timeline_vertical",
    "hub_and_spoke",
    "split_left_right",
    "split_top_bottom",
    "compare_dual_axis",
    "process_horizontal",
    "process_vertical",
    "hero_with_supporting_cards"
  ],
  "element_primitives": [
    "rounded_card",
    "value_tag",
    "number_badge",
    "linear_icon",
    "arrow_connector",
    "dashed_feedback_line"
  ],
  "variation_policy": {
    "same_layout_max_repeat": 1,
    "min_distinct_layout_families": 3,
    "allow_local_recomposition": true
  },
  "negative_rules": [
    "不要写实人物海报风",
    "不要复杂炫光背景",
    "不要突然切成深色大屏风"
  ],
  "prompt_anchor": "cyberpunk_corporate",
  "prompt_compression": {
    "mode": "anchor_plus_core_constraints",
    "max_prompt_chars": 1800
  }
}
```

### 2.2 页面蓝图升级

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:233) 的 `normalize_content_plan()` 输出。

每页新增字段：

- `layout_family`
- `layout_slots`
- `element_plan`
- `difference_from_previous`
- `style_constraints`
- `reference_mode`
- `prompt_profile`

建议结构：

```json
{
  "page_no": 1,
  "title": "页面标题",
  "summary": "页面摘要",
  "bullets": ["要点1", "要点2"],
  "layout_family": "split_left_right",
  "layout_slots": {
    "title": "top_left",
    "content_blocks": ["left_text", "right_visual"],
    "visual_focus": "right_center"
  },
  "element_plan": {
    "primitives": ["rounded_card", "linear_icon", "arrow_connector"],
    "icon_topics": ["检索", "筛选", "决策"],
    "diagram_type": "comparison_flow"
  },
  "difference_from_previous": "避免继续使用中心主视觉，改为左右分栏信息对比",
  "style_constraints": {
    "inherit_style_only": true,
    "reuse_composition": false
  },
  "reference_mode": "generation",
  "prompt_profile": "compressed"
}
```

### 2.3 新增独立模块

新增 [ppt_system/design_grammar.py](/d:/桌面/ppt生成/ppt_system/design_grammar.py)。

职责：

- 定义默认版式家族
- 定义元素原语集合
- 定义风格固定层与变化层
- 提供 grammar 归一化函数
- 提供 layout family 合法性校验
- 提供 prompt 压缩输入结构

---

## 3. 版式家族抽象规范

这是本次改造的强约束，不允许降级成枚举编号。

### 3.1 命名原则

`layout_family` 必须表达“排版模式”，而不是“某个具体模板编号”。

允许：

- `grid_n_x_m`
- `timeline_horizontal`
- `timeline_vertical`
- `hub_and_spoke`
- `split_left_right`
- `split_top_bottom`
- `process_horizontal`
- `process_vertical`
- `compare_dual_axis`
- `hero_with_supporting_cards`

不允许：

- `layout_1`
- `layout_2`
- `template_a`
- `style_page_3`

### 3.2 设计要求

- `layout_family` 只定义抽象结构，不绑定具体像素坐标
- `layout_slots` 只定义语义槽位，不定义最终绝对布局
- 最终坐标由大模型根据槽位、内容密度、图文比例自动推断
- 本地规则层只做兜底，不做重度硬编码排版

### 3.3 坐标生成策略

在 [ppt_system/text_layout.py](/d:/桌面/ppt生成/ppt_system/text_layout.py:38) 中改为两层：

1. family -> slots
2. slots -> fallback boxes

也就是：

- 第一层产出语义布局骨架
- 第二层只在模型未提供足够信息时生成兜底坐标

这样可以利用大模型能力自动计算布局，而不是把系统重新做成模板拼图机。

---

## 4. 风格分析阶段改造

### 4.1 改造 `build_style_analysis_prompt()`

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:78)。

当前只抽取 `palette/layout_language/must_keep/avoid/style_anchor`，要升级成输出：

- `style_core.background_tone`
- `style_core.palette`
- `style_core.title_style`
- `style_core.card_style`
- `style_core.icon_style`
- `style_core.line_style`
- `layout_families`
- `element_primitives`
- `variation_policy`
- `negative_rules`
- `prompt_anchor`

新增要求：

- `layout_families` 要总结参考图对应的高层排版模式
- 不要返回编号式模板名
- `prompt_anchor` 要能概括整套风格，用于 prompt 压缩

### 4.2 改造 `fallback_style_guide()`

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:111)。

要求：

- 没有参考图时也能产出完整 grammar
- fallback 不能只给“商务科技风”这种大词
- 必须给出默认 `layout_families`
- 必须给出默认 `element_primitives`
- 必须给出默认 `prompt_anchor`
- 必须给出 `prompt_compression` 默认策略

### 4.3 改造 `normalize_style_guide()`

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:148)。

要求：

- 对新增结构逐项校验
- 非法 family 名称自动替换为允许集合中的抽象模式
- 去掉编号式 family 名称
- 缺失字段自动补默认值
- 输出保证可以被后续 prompt 模块直接消费

---

## 5. 规划阶段改造

### 5.1 改造 `build_planning_prompt()`

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:179)。

新增明确约束：

- 每页必须选择一个 `layout_family`
- 相邻页不能重复同一 `layout_family`
- 整套页至少覆盖 3 种以上骨架
- 必须继承 `element_primitives`
- 必须按本页内容重新生成具体图形
- 不允许复用参考图的具体构图
- `layout_family` 必须是抽象排版模式，不能写成模板编号

### 5.2 改造规划 JSON 输出格式

页面输出建议包含：

- `layout_family`
- `layout_slots`
- `element_plan.primitives`
- `element_plan.icon_topics`
- `element_plan.diagram_type`
- `difference_from_previous`
- `reference_mode`
- `prompt_profile`

### 5.3 改造 `normalize_content_plan()`

位置在 [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py:233)。

增加页面蓝图兜底逻辑：

- 如果模型没返回 `layout_family`，按内容类型自动推断
- 如果 `layout_family` 不合法，自动映射到最近的抽象 family
- 如果相邻页重复，自动换成下一个 family
- 如果 `element_plan` 缺失，按 `style_guide.element_primitives` 补齐
- 如果 `prompt_profile` 缺失，默认走 `compressed`

### 5.4 在 `planner.py` 增加辅助函数

位置在 [ppt_system/planner.py](/d:/桌面/ppt生成/ppt_system/planner.py:7)。

建议新增：

- `infer_layout_family()`
- `dedupe_layout_sequence()`
- `normalize_layout_family_name()`
- `build_default_element_plan()`
- `infer_reference_mode()`

---

## 6. 文本布局层改造

### 6.1 改造 `text_layout.py`

位置在 [ppt_system/text_layout.py](/d:/桌面/ppt生成/ppt_system/text_layout.py:38)。

当前默认只生成左侧文字区，这会压缩版式多样性。

需要改成：

- 根据 `layout_family` 产出不同文本框骨架
- family 只决定语义槽位，不强绑定具体像素
- 坐标只作为 fallback，不作为主导版式机制

优先支持的 family：

- `split_left_right`
- `split_top_bottom`
- `timeline_horizontal`
- `timeline_vertical`
- `hub_and_spoke`
- `grid_n_x_m`
- `process_horizontal`
- `process_vertical`
- `hero_with_supporting_cards`

### 6.2 新增文本骨架映射函数

建议放在 [ppt_system/text_layout.py](/d:/桌面/ppt生成/ppt_system/text_layout.py)：

- `build_layout_slots_by_family()`
- `build_text_boxes_from_slots()`
- `build_fallback_boxes_for_family()`

### 6.3 保留兜底布局

保留当前兜底布局，但只作为最后 fallback，不再作为默认主路径。

---

## 7. 第一阶段 Prompt 改造

### 7.1 改造 `build_reference_prompt()`

位置在 [ppt_system/generation_prompts.py](/d:/桌面/ppt生成/ppt_system/generation_prompts.py:6)。

从“单段描述”升级成“结构化拼装”：

- 风格固定层
- 本页版式骨架
- 元素语言要求
- 本页内容要求
- 禁止事项

### 7.2 提示词压缩层

这是本次改造的第二个强约束。

如果最终组装的 prompt 超过预期长度，不要把完整 grammar 字典全量拼进去，而要先经过压缩层。

建议新增函数：

- `build_prompt_anchor(style_guide)`
- `compress_style_guide(style_guide, max_chars)`
- `build_prompt_profile(page, style_guide)`

建议放在：

- [ppt_system/design_grammar.py](/d:/桌面/ppt生成/ppt_system/design_grammar.py)
- 或 [ppt_system/generation_prompts.py](/d:/桌面/ppt生成/ppt_system/generation_prompts.py)

### 7.3 压缩策略

推荐分三级：

1. `full`
   - 用于调试
   - 传完整 grammar 与详细约束

2. `core`
   - 只传 `style_core + layout_family + element_primitives + negative_rules`

3. `compressed`
   - 传 `prompt_anchor + 核心约束短句 + 本页差异要求`

压缩后的结构示例：

```text
风格锚点：cyberpunk_corporate
核心约束：白底或浅蓝灰底，深海军蓝标题，亮蓝强调，浅蓝描边圆角卡片，扁平线性图标，清晰箭头与虚线反馈链路。
本页骨架：split_left_right
差异要求：与上一页不同，不使用中心聚焦构图。
```

### 7.4 改造 `merge_prompt_with_style_lock()`

位置在 [ppt_system/generation_prompts.py](/d:/桌面/ppt生成/ppt_system/generation_prompts.py:31)。

要求：

- 保留现有 `style_anchor`
- 增加 `style_core` 明确描述
- 增加 `layout_families` 与当前页 `layout_family` 的关系
- 增加“继承风格，不继承具体构图”
- 增加“继承元素语言，不复用具体图形”
- 当 prompt 过长时自动降级到 `core` 或 `compressed`

### 7.5 将 prompt 约束改成“固定层 + 变化层”

固定层：

- 背景明度
- 色板
- 卡片风格
- 图标风格
- 连线风格

变化层：

- 模块数量
- 局部构图
- 图标主题
- 图形实例

---

## 8. 第二阶段 Prompt 改造

### 8.1 改造 `build_elements_prompt()`

将 [ppt_system/generation_prompts.py](/d:/桌面/ppt生成/ppt_system/generation_prompts.py:54) 的 `build_elements_prompt()` 从固定字符串改成动态函数。

建议签名：

- `build_elements_prompt(page, style_guide) -> str`

### 8.2 第二阶段 prompt 约束

新增这些要求：

- 仅删除文字
- 保留卡片、图标、箭头、编号、标签、容器
- 保持原有层级和相对位置
- 保留描边粗细和圆角风格
- 保留流程关系和反馈链路
- 不要重绘成另一套素材风格
- 尽量继承一阶段的 `prompt_anchor`

### 8.3 在 `web_app.py` 改成按页生成

位置在 [web_app.py](/d:/桌面/ppt生成/web_app.py:922)。

要求：

- 元素图 prompt 不再全局复用同一句
- 每页根据 `layout_family`、`element_plan`、`style_core` 动态生成

---

## 9. 参考图使用策略改造

### 9.1 梳理一阶段生图策略

位置在 [ppt_system/openai_image_provider.py](/d:/桌面/ppt生成/ppt_system/openai_image_provider.py:40)。

当前问题：

- 有参考图时直接走 edit API，容易把构图也继承进去

### 9.2 调整策略

- 普通内容页优先使用 `generation`
- 仅封面页或少数风格锚点页考虑 `edit_with_refs`
- 即使用 refs，也在 prompt 中明确“只继承视觉语言，不复用原构图”

### 9.3 页面蓝图增加字段

新增：

- `reference_mode: generation | edit_with_refs`

### 9.4 在 `web_app.py` 按页决定是否传参考图

位置在 [web_app.py](/d:/桌面/ppt生成/web_app.py:838)。

---

## 10. 评估闭环改造

### 10.1 新增评估模块

新增 [ppt_system/page_evaluator.py](/d:/桌面/ppt生成/ppt_system/page_evaluator.py)。

职责：

- 评估风格一致度
- 评估版式差异度
- 评估元素语言一致度
- 评估内容表达度
- 评估文字可读性风险

### 10.2 第一版规则评估

先做轻量规则评估：

- 相邻页 `layout_family` 重复则低分
- 页面 prompt 未覆盖核心 primitives 则低分
- 背景明度词偏离 `style_core.background_tone` 则低分
- 出现 `negative_rules` 中的禁忌词则低分
- family 命名不符合抽象模式则低分
- 压缩后 prompt 丢失关键风格约束则低分

### 10.3 接入阶段完成后的评分

在 [web_app.py](/d:/桌面/ppt生成/web_app.py:858) 和 [web_app.py](/d:/桌面/ppt生成/web_app.py:985) 的阶段完成后接入评分。

低于阈值时：

- 记录原因
- 自动重试一次
- 第二次仍失败则保留结果并告警

---

## 11. 前端与状态展示改造

### 11.1 任务结果页增加结构化信息

位置在 [front/static/app.js](/d:/桌面/ppt生成/front/static/app.js) 和 [front/templates/index.html](/d:/桌面/ppt生成/front/templates/index.html)。

建议展示：

- 本页 `layout_family`
- 本页 `element_plan`
- 本页 `reference_mode`
- 本页 `prompt_profile`
- 自动评估分数

### 11.2 增加“风格 grammar”折叠面板

让你们能直接看：

- `style_core`
- `layout_families`
- `element_primitives`
- `negative_rules`
- `prompt_anchor`
- `prompt_compression`

这样后面调 prompt 不需要只盯最终图片猜原因。

---

## 12. 配置项改造

在 [config.json](/d:/桌面/ppt生成/config.json:1) 增加通用开关：

- `enable_design_grammar`
- `min_distinct_layout_families`
- `max_adjacent_layout_repeat`
- `stage1_reference_strategy`
- `enable_page_evaluation`
- `page_evaluation_retry_count`
- `prompt_compression_mode`
- `prompt_max_chars`

保持默认值保守，确保兼容旧流程。

建议：

- grammar 默认开启
- evaluation 默认开启但只告警不拦截
- reference strategy 默认 `hybrid`
- prompt compression 默认 `compressed`

---

## 13. 测试清单

### 13.1 `design_grammar` 单元测试

覆盖：

- 空参考图
- 单张参考图
- 多张参考图风格冲突
- 缺字段自动补齐
- family 名称不合法时自动归一化
- prompt anchor 生成是否稳定

### 13.2 `normalize_content_plan()` 测试

覆盖：

- 模型漏返回 `layout_family`
- 连续三页版式重复
- `element_plan` 缺失
- 页面数量不够
- `layout_family` 返回成 `layout_1` 时自动修正

### 13.3 `text_layout.py` 测试

覆盖：

- 每种 `layout_family` 至少产出一套合法文本框
- 不同骨架的文本区坐标明显不同
- fallback 坐标只在必要时触发

### 13.4 prompt 构建测试

覆盖：

- 一阶段 prompt 包含固定层和变化层
- 二阶段 prompt 包含元素保真约束
- prompt 中出现“继承风格，不继承构图”的关键语义
- 超长 prompt 会自动压缩
- `full/core/compressed` 三档切换正确

### 13.5 泛化回归样例

至少做 3 组，不只测单一案例：

- 企业蓝白流程图风
- 深色科技界面风
- 极简白底咨询图表风

这一步很重要，能避免过拟合当前样例。

---

## 14. 实施顺序

### 第一批

先改“数据结构 + prompt”，不碰模型调用策略。

涉及：

- [ppt_system/content_agent.py](/d:/桌面/ppt生成/ppt_system/content_agent.py)
- [ppt_system/generation_prompts.py](/d:/桌面/ppt生成/ppt_system/generation_prompts.py)
- 新增 [ppt_system/design_grammar.py](/d:/桌面/ppt生成/ppt_system/design_grammar.py)

### 第二批

改“文本布局 + 页面蓝图兜底”。

涉及：

- [ppt_system/text_layout.py](/d:/桌面/ppt生成/ppt_system/text_layout.py)
- [ppt_system/planner.py](/d:/桌面/ppt生成/ppt_system/planner.py)

### 第三批

改“参考图策略 + 元素图动态 prompt”。

涉及：

- [ppt_system/openai_image_provider.py](/d:/桌面/ppt生成/ppt_system/openai_image_provider.py)
- [web_app.py](/d:/桌面/ppt生成/web_app.py)

### 第四批

补“评估闭环 + 前端可视化”。

涉及：

- 新增 [ppt_system/page_evaluator.py](/d:/桌面/ppt生成/ppt_system/page_evaluator.py)
- [front/static/app.js](/d:/桌面/ppt生成/front/static/app.js)
- [front/templates/index.html](/d:/桌面/ppt生成/front/templates/index.html)

---

## 15. 验收标准

用同一组参考图生成 5 页 PPT，满足：

- 至少 3 种不同 `layout_family`
- `layout_family` 全部是抽象排版模式，不出现模板编号
- 主色、背景明度、卡片体系稳定
- 元素语言一致
- 相邻页视觉骨架明显不同
- 没有明显“抄原图构图”
- 第二阶段去字后仍保留流程关系和组件层级
- 压缩模式下的 prompt 仍能保持风格稳定，不因缩短而明显跑偏

---

## 16. 额外实现提醒

- `layout_family` 是设计语言，不是模板 ID
- `layout_slots` 是语义槽位，不是固定像素坐标
- prompt 压缩的目标不是删信息，而是保留最强识别信号
- `prompt_anchor` 要尽量短、稳、可迁移，避免写成一句长口号
- 任何 fallback 都应是通用兜底，而不是为当前参考图写死
