# 移动端源码

该目录与 `web_ui` 桌面端前端隔离，移动端使用独立的 Vite/React 工程，默认开发地址为 `http://电脑IP:7861`。

移动端通过桌面端的 `/api`、`/runs` 和 `/output` 接口访问任务、预览资源和导出文件；不会复制或直接读取桌面端的 API Key、配置文件和历史数据库。

后续可在此目录继续实现完整的移动端任务创建、规划编辑、版式切换、图表录入、页面编辑和 PPT 下载流程。
